package com.dermacon.jwtauth.exception;

/**
 * The subErrors property holds an array of sub-errors that happened. This is used for
 * representing multiple errors in a single call. An example would be validation errors in which
 * multiple fields have failed the validation. The ApiSubError class is used to encapsulate those.
 *
 * source: https://www.toptal.com/java/spring-boot-rest-api-error-handling
 */
public abstract class ApiSubError {
}
